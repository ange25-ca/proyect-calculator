import React from 'react';
import  ReactDOM  from 'react-dom/client';
import Calculador from './assets/Calculador';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Calculador />
  </React.StrictMode>
);